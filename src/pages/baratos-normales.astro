---
import Layout from '../layouts/Layout.astro';
import messi from '../components/Messi.js';
---

<Layout title='Baratos y Normales'>
  <main>
    <h1>Dominios</h1>
    <a href='/'> Home</a>
    <ul>
      {
        messi
          .sort((a, b) => {
            return a.register - b.register;
          })
          .map((m) => {
            if (m.type != 'handshake') {
              return (
                <li>
                  <h2>{m.tld}</h2>
                  <h3>{m.register}</h3>
                </li>
              );
            }
          })
      }
    </ul>
  </main>
</Layout>
<style>
  main {
    width: 100vw;
    min-height: 100vh;
  }
  h1 {
    width: 100vw;
    text-align: center;
  }
  ul {
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
    justify-content: space-evenly;
    align-items: center;
    width: 90%;
  }
  li,
  h2,
  h3 {
    max-width: fit-content;
    padding: 1px;
    text-align: center;
  }
  li {
    border: 1px solid black;
    margin: 1px;
    list-style: none;
  }
  h3 {
    color: red;
  }
</style>
